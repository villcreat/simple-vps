# Portfolio Website Scenario

Implemented in `lib/src/data/install_scenarios.dart`
(`InstallScenarios.portfolio`), built from the shared `_staticSite` recipe:
Nginx + a static page under `/var/www/portfolio` on port 80, with rollback for
the dangerous steps.
