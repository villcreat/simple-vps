# Resume Website Scenario

Implemented in `lib/src/data/install_scenarios.dart` (`InstallScenarios.resume`),
built from the shared `_staticSite` recipe: Nginx + a static page under
`/var/www/resume` on port 80, with rollback for the dangerous steps.
