# Desktop App

The Flutter root project currently hosts the shared app code. Generate desktop
platform runners with Flutter when the SDK is installed:

```powershell
flutter create --platforms=windows,linux,macos .
```
